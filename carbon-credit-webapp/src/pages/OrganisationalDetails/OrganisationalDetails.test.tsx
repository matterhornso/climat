import React from 'react'
import { render, screen } from '@testing-library/react'
import OrganisationalDetails from './OrganisationalDetails'

test('renders OrganisationalDetails', () => {
  // render(<App />);
  // const linkElement = screen.getByText(/OrganisationalDetails/i);
  // expect(linkElement).toBeInTheDocument();
})
