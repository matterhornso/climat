import React from "react";
import { render, screen } from "@testing-library/react";
import TwoFa from './TwoFa';

test("renders TwoFa", () => {
  // render(<App />);
  // const linkElement = screen.getByText(/TwoFa/i);
  // expect(linkElement).toBeInTheDocument();
});
