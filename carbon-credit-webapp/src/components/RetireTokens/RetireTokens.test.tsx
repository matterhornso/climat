import React from 'react'
import { render, screen } from '@testing-library/react'
import RetireTokens from './RetireTokens'

test('renders RetireTokens', () => {
  // render(<App />);
  // const linkElement = screen.getByText(/RetireTokens/i);
  // expect(linkElement).toBeInTheDocument();
})
