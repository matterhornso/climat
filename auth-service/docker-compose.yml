version: "2"
services:
  web:
    build: .
    command: npm start
    ports:
      - "3000:3000"
    depends_on:
      - mongo
    environment:
      - MONGODB_HOST=mongodb://mongo:27017/
      - DB_NAME=shine-dev
  mongo:
    image: mongo
    ports:
      - "27015:27017"
