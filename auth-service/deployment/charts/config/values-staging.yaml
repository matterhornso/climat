# Default values for  application in staging environment.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
environment:
  name: staging

replicaCount: 1

image:
  repository: registry.gitlab.com/shine-traceability/auth-service

service:
  type: NodePort
  port: 80

containers:
  port: 3001

environmentVariables: {}

podAntiAffinity:
  enabled: true
